# Docker profile: convenient defaults for local Docker runs on Mac

# Default to host.docker.internal so the container can reach host DB
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://host.docker.internal:3306/chatdb}
